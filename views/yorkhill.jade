
extends layout


block content
  h1= title
  div.container
            ul.col-md-12.row
                a(href="/")
                    li.col-md-3.col-md-offset-1 Home Page
                a(href="/maincampus")
                    li.col-md-3 Main Campus 
  h3 Hours of Operation:
   p Monday - Thursday 7am-9pm
   p Friday            7am-7pm
   p Saturday/Sunday   9am-7pm
  - var counter = 0;
  p Select an option below to view the amount of machines in use.

  

  button(onclick='removeperson()') Remove a person.
  button(onclick='addperson()') Add a person.

  script.
    var inUse = 0
    var ms = #{machines}
    function removeperson(e) {
      inUse -= 1
      if (inUse < 20) {
        alert(` machine #${ms - inUse} assigned`)
      } else {
        alert('All machines are taken, please try again later.')
      }
    }
    function addperson(e) {
      inUse += 1
      if (inUse < 20) {
        alert(` machine #${ms + inUse} assigned`)
      } else {
        alert('All machines are taken, please try again later.')
      }
    }

  h1 News and Announcements:
    h3 The fitness center will be closed for Thanksgiving break!





